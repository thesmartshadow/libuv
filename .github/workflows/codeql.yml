name: "CodeQL Advanced"

on:
  push:
    branches: [ "v1.x" ]
  pull_request:
    branches: [ "v1.x" ]
  schedule:
    - cron: '27 13 * * 0'

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ${{ (matrix.language == 'swift' && 'macos-latest') || 'ubuntu-latest' }}
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
        - language: c-cpp
          build-mode: manual # تم تغيير هذه القيمة إلى manual
        - language: python
          build-mode: none
          
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    # خطوة جديدة: تثبيت الأدوات اللازمة لعملية البناء لمشروع libuv
    - name: Install build dependencies
      if: matrix.language == 'c-cpp'
      run: sudo apt-get update && sudo apt-get install -y autoconf automake libtool make gcc

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v4
      with:
        languages: ${{ matrix.language }}
        build-mode: ${{ matrix.build-mode }}

    # تم وضع أوامر بناء المشروع هنا
    - name: Run manual build steps
      if: matrix.build-mode == 'manual'
      shell: bash
      run: |
        echo "Starting manual build for libuv..."
        sh autogen.sh
        ./configure
        make

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v4
      with:
        category: "/language:${{matrix.language}}"
